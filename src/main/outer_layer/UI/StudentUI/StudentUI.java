package outer_layer.UI.StudentUI;

import outer_layer.UI.GetMethod;

public class StudentUI implements GetMethod {
    // TODO: put all the stuff related to the student UI here
    // may link to another UI which includes other tables we discuss during the meeting.
    @Override
    public void get_method() {
        // TODO: put method body here.
    }
}
